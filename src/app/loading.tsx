export default function Loading() {
  return (
    <main className="min-h-screen px-6 py-10 sm:px-8">
      <section className="mx-auto w-full max-w-5xl border border-blue-200 bg-blue-50 p-6 text-blue-950">
        <p className="text-sm font-semibold uppercase tracking-[0.16em]">Loading</p>
        <h1 className="mt-3 text-2xl font-semibold">Preparing the report workspace...</h1>
      </section>
    </main>
  );
}
